<section>
  <h1 class="text-2xl font-bold"><%= @board.name %></h1>

  <section class="flex gap-5">
    <%= render @board.lists %>
    <%= form_with model: [@board, @board.lists.build] do |form| %>
      <section class="bg-gray-300 p-2 rounded h-min">
        <%= form.submit "New list +" %>
      </section>
    <% end %>


  </section>

</section>